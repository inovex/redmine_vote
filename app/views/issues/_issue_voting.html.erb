<% if show_votes?(@issue) %>
  <%= issue_fields_rows do |rows|
    voting_controls = content_tag(:span, id: :voting_controls) do
      render(:partial => "issues/voting_controls", :locals => { :issue => @issue, :success => nil })
    end

    rows.left l(:label_votes), voting_controls
  end %>
<% end %>
